import 'package:flutter/material.dart';

class AppColors {
  // Primary colors inspired by Tunisian culture
  static const Color primary = Color(
    0xFF1B4B8C,
  ); // Deep blue like Mediterranean
  static const Color primaryLight = Color(0xFF4A6FA5);
  static const Color primaryDark = Color(0xFF0F2A5A);

  // Secondary colors
  static const Color secondary = Color(
    0xFFD4AF37,
  ); // Gold like traditional crafts
  static const Color secondaryLight = Color(0xFFE6C866);
  static const Color secondaryDark = Color(0xFFB8941F);

  // Accent colors
  static const Color accent = Color(0xFFE74C3C); // Warm red
  static const Color accentLight = Color(0xFFEC7063);
  static const Color accentDark = Color(0xFFCB4335);

  // Neutral colors
  static const Color background = Color(0xFFFAFAFA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF5F5F5);

  // Text colors
  static const Color textPrimary = Color(0xFF2C3E50);
  static const Color textSecondary = Color(0xFF7F8C8D);
  static const Color textLight = Color(0xFFBDC3C7);

  // Status colors
  static const Color success = Color(0xFF27AE60);
  static const Color warning = Color(0xFFF39C12);
  static const Color error = Color(0xFFE74C3C);
  static const Color info = Color(0xFF3498DB);

  // Border and divider colors
  static const Color border = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFEEEEEE);

  // Shadow colors
  static const Color shadow = Color(0x1A000000);
  static const Color shadowLight = Color(0x0D000000);
}
